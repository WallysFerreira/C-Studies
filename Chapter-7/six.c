#include <stdio.h>

int main(void) {
	printf("Size of int: %d\nSize of short: %d\nSize of long: %d\nSize of float: %d\nSize of double: %d\nSize of long double: %d\n", (int) sizeof (int), (int) sizeof (short), (int) sizeof (long), (int) sizeof (float), (int) sizeof (double), (int) sizeof (long double));

	return 0;
}
